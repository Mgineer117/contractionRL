seed: 42

agent:
  class: SDLQR
  Q_scaler: 1.0
  R_scaler: 0.0

trainer:
  class: EvalTrainer
  num_agent: 4
  epochs: 1        # SD-LQR is analytical — one eval pass is sufficient
  eval_interval: 1
  log_interval: 1
